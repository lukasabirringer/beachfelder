<div class="row">
  <div class="column column--12">
    <h2 class="title-page__title">Beachvolleyballfelder in {{ $name }}</h2>
    @if( $description )
    	<p class="title-page__subtitle">{{ $description }}</p>
    @endif
  </div>
</div>